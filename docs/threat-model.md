# CDS Production Threat Model and Penetration-Test Plan

Version: 1.0
Status: Draft — reviewed by maintainers before controls in issues #205, #206, #207, #210, #211, and #216 are implemented against it.
Related: [#212](https://github.com/RonaldHensbergen/composable-data-stack/issues/212) (this document), [#64](https://github.com/RonaldHensbergen/composable-data-stack/issues/64) (production-readiness epic).

## 1. Purpose and scope

This document defines the assets, trust boundaries, attacker capabilities, and
prioritized risks for a CDS-rendered production deployment, and derives a
repeatable penetration-test plan from that model. It exists to give the
remaining production-hardening issues (#205, #206, #207, #210, #211, #216) a
shared, testable set of threats to design controls against, rather than each
issue inventing its own threat assumptions.

**In scope:**

- The CDS CLI itself (`validate` / `plan` / `render` / `test` / `security` /
  `up`) and the artifacts it produces (rendered Compose files, plans,
  diagnostics).
- Modules shipped in `modules/` (currently Postgres, Superset, KeyDB, Dagster,
  Vault, and any added afterward) and their generated runtime configuration.
  This model is expressed in terms of module **categories** (warehouse,
  BI, cache, orchestration, secrets, and future categories such as
  transformation or quality) rather than fixed module names, so it remains
  valid as new modules are added without requiring a rewrite.
- Profiles and environment overlays (`profiles/**/profile.yaml`,
  `profiles/**/environments/*.yaml`) and the secret-resolution path
  (`cli/secrets.py`, `${CDS_*}` placeholders).
- CI/CD supply chain for CDS-published images (`.github/workflows/publish-images.yml`,
  `.github/workflows/image-security-scan.yml`), including signing/provenance
  (`cli/image_verification.py`).
- The Docker host and daemon underneath a rendered stack, to the extent CDS
  can inspect or influence it (rootless mode, socket exposure, capabilities).

**Out of scope (tracked separately):**

- Kubernetes-specific attack surface — tracked under #65 and does not block
  the Docker Compose production baseline.
- Vulnerabilities in third-party module images themselves (Postgres, Superset,
  Dagster upstream code) beyond CDS's ability to pin, scan, and verify them.
- Physical/cloud-provider infrastructure security outside the host running
  Docker.

## 2. Assets

> **Maintenance note:** the module examples below (Postgres, Superset, KeyDB,
> Dagster, Vault) are illustrative, not exhaustive. When a new module category
> or implementation is added under `modules/`, add its assets to this table
> under the closest existing category (warehouse, BI, cache, orchestration,
> secrets) or introduce a new category row rather than editing the threat
> model on a per-module basis for every addition.

| Asset | Description | Confidentiality | Integrity | Availability |
| --- | --- | --- | --- | --- |
| Secrets (DB passwords, API keys, Vault tokens) | Values resolved via `${CDS_*}` from `.env`/environment, referenced (never embedded) in profiles | Critical | High | Medium |
| Rendered Compose output | `docker-compose.yml` generated by `cds render`/`cds up` | Medium | High | Medium |
| Profile/module source (YAML) | `profiles/**/*.yaml`, `modules/**/module.yaml` — the declarative input to the compiler | Low | High | Medium |
| Warehouse data (any `warehouse` category module, e.g. Postgres) | Analytical/application data managed by the module | Critical | Critical | High |
| Orchestration metadata (any `orchestration` category module, e.g. Dagster) | Run history, schedules, credentials used to reach other systems | High | High | Medium |
| BI metadata and dashboards (any `bi` category module, e.g. Superset) | Saved queries, dashboards, cached credentials to the warehouse | High | Medium | Medium |
| Cache contents (any `cache` category module, e.g. KeyDB) | Ephemeral cache/session data, may include tokens | Medium | Low | Medium |
| Container images (ghcr.io, Docker Hub) | Published CDS runtime images and pulled third-party base images | High | Critical | Medium |
| CI/CD pipeline and its secrets | GitHub Actions workflows, `GITHUB_TOKEN`, OIDC identity used for keyless signing | Critical | Critical | Medium |
| Docker host and daemon | The machine executing `docker compose` | Critical | Critical | Critical |
| CLI diagnostics/plan output | Text shown to operators or captured in logs/CI output | High | Low | Low |

## 3. Trust boundaries

```text
┌─────────────────────────────────────────────────────────────────┐
│ Operator workstation / CI runner                                 │
│                                                                   │
│  ┌───────────┐   render/plan   ┌────────────────────┐            │
│  │  Profile   │ ─────────────► │   CDS CLI process   │            │
│  │  YAML repo │                │ (validate/plan/     │            │
│  └───────────┘                 │  render/security)    │            │
│        ▲                       └─────────┬────────────┘            │
│        │ secret refs                     │ writes                  │
│  ┌───────────┐                            ▼                        │
│  │ .env /     │◄──── resolved at    ┌─────────────────┐            │
│  │ environment│      compose time   │ docker-compose  │            │
│  └───────────┘                      │     .yml        │            │
└──────────────────────────────────────┴───────┬─────────┘────────────┘
                                                │ docker compose up
                          Trust boundary A (host/daemon) 
┌───────────────────────────────────────────────▼─────────────────────┐
│ Docker host + daemon                                                 │
│                                                                       │
│   ┌────────────┐   ┌────────────┐   ┌───────────┐   ┌─────────────┐ │
│   │  warehouse  │   │     bi      │   │   cache   │   │orchestration│ │
│   │  (Postgres, │◄─►│  (Superset, │   │ (KeyDB,   │◄─►│  (Dagster,  │ │
│   │   etc.)     │   │   etc.)     │   │  etc.)    │   │   etc.)     │ │
│   └────────────┘   └────────────┘   └───────────┘   └─────────────┘ │
│         ▲ internal service network (trust boundary B, per-profile)  │
│         │       + any future module category added under modules/    │
│   ┌────────────┐                                                     │
│   │  secrets    │  (e.g. Vault, optional)                             │
│   └────────────┘                                                     │
└───────────────────────────────────────────────────────────────────────┘
                          Trust boundary C (registry pull)
┌───────────────────────────────────────────────────────────────────────┐
│ Container registries (ghcr.io, docker.io) + CI publish pipeline        │
│   build → scan → sign (cosign) → SBOM → publish                        │
└─────────────────────────────────────────────────────────────────────────┘
```

Named boundaries used throughout this document:

- **Boundary A — CLI/host boundary.** Between the machine running `cds` (an
  operator laptop or a CI runner) and the Docker daemon it drives. CDS itself
  never talks to the daemon directly (`cds up` shells out to `docker
  compose`), so this boundary is crossed once per `docker compose` invocation.
- **Boundary B — inter-service network boundary.** Between containers on the
  Compose-rendered network. Contracts (`consumes`/`provides`) are the only
  sanctioned way one module's service reaches another's.
- **Boundary C — registry/supply-chain boundary.** Between the CI pipeline
  that builds/signs/publishes CDS images and the host that later pulls and
  verifies them via `cli/image_verification.py`.
- **Boundary D — secret boundary.** Between the operator's secret store
  (`.env`, environment, or a secrets module like Vault) and everything CDS
  ever writes to disk or stdout. CDS's compile-time code path is designed to
  never cross this boundary with a resolved value (see `CDS-SEC-006`,
  `CDS-SEC-030`, `CDS-SEC-071`).

## 4. Threat actors

| Actor | Capability | Motivation |
| --- | --- | --- |
| **External network attacker** | No credentials; can reach any port the host exposes to a routable network | Data theft, ransomware, botnet recruitment |
| **Malicious/compromised dependency** | Can execute code inside a build or at runtime inside one container (e.g. compromised base image, malicious PyPI package pulled at container build time) | Supply-chain compromise, lateral movement |
| **Malicious/compromised CI job** | Can execute arbitrary code with the CI runner's permissions, including the `GITHUB_TOKEN` and OIDC token used for keyless signing | Publish a tampered/backdoored image under a trusted identity |
| **Insider with repo write access** | Can modify profile/module YAML, `rule-set.json`, or CI workflow files | Weaken security policy, exfiltrate secrets, plant malicious module config |
| **Compromised operator workstation/laptop** | Has the operator's `.env`/environment secrets and can run `cds`/`docker` locally | Direct access to all resolved secrets and local Docker socket |
| **Malicious/misconfigured co-tenant on shared host** | Runs another workload on the same Docker host/daemon | Container escape, resource exhaustion, socket abuse |
| **Curious/careless operator** | Legitimate access; may misconfigure a production profile (e.g. picks `local` overlay for a public deployment) | Unintentional exposure, not malicious but treated as a risk source |

## 5. Attack paths and prioritized risks

Each attack path lists the boundary it crosses, current CDS controls (rule ID
or code reference where one exists), and the residual risk driving one or
more of the follow-up issues.

### 5.1 Secrets

| # | Attack path | Boundary | Current control | Residual risk / owning issue |
| --- | --- | --- | --- | --- |
| T1 | Hardcoded secret committed to a profile/module YAML | D | `CDS-SEC-001`–`004`, `CDS-SEC-033` | Detection only for known patterns; no repo-wide secret scanning in CI. Tracked outside this milestone. |
| T2 | Resolved secret leaks into `cds plan`/`validate` stdout or a generated Compose file | D | `CDS-SEC-006`, `CDS-SEC-030`, `CDS-SEC-071` | Covered; regression tests exist. Re-verify under #216 process-manager change (new log surfaces). |
| T3 | Secret passed via container `command:` args, visible in `docker inspect`/process list | B/D | `CDS-SEC-070` scope bug (issue #297) | **High** — rule currently never fires (dead scope). Needs fix before #206/#216 can rely on it. |
| T4 | Weak/default/reused credentials (DB, admin UI, cache) | B | `CDS-SEC-010`–`013` | `CDS-SEC-010`'s first branch is dead code (#292); partial coverage only. **Owning issue: #206.** |
| T5 | No authentication required on relational DB / cache in production | B | None enforced today; `keydb` password field exists but `connectionUri` doesn't embed it (#291) | **High** — no production-mode enforcement that auth is mandatory. **Owning issue: #206.** |
| T6 | Env var falls back to an insecure default when unset (`${VAR:-admin}`) | D | `CDS-SEC-040`/`041`, but issue #295 notes `${VAR:-fallback}` with a hardcoded insecure default isn't flagged by `cds preflight` | Medium — partially covered by security rules, gap in preflight. |
| T7 | Secret provider unreachable/misconfigured, workload silently runs with no secret | D | `CDS-SEC-011` (missing required auth secret) | Covered for known contract fields. |

### 5.2 Network and service exposure

| # | Attack path | Boundary | Current control | Residual risk / owning issue |
| --- | --- | --- | --- | --- |
| T8 | Admin UI (any `bi`/admin-facing module, e.g. Superset) or DB port bound to `0.0.0.0` and reachable externally | A/B | `CDS-SEC-020`, `CDS-SEC-021` | Covered by rule; no network segmentation/allowlist model exists yet. **Owning issue: #70 (integration), #205 (TLS-adjacent).** |
| T9 | No internal/external network segmentation — all services on one flat network | B | None — current Compose renders one network per profile | **High** — flat network means a compromised low-value service (e.g. cache) can reach the warehouse directly. **Owning issue: #70.** |
| T10 | Plaintext HTTP between services or to operator, credentials/session tokens sniffable on Boundary B | B | `CDS-SEC-023` (authenticated service uses plain HTTP) flags some cases; no TLS contract exists | **High** — no first-class TLS modeling. **Owning issue: #205.** |
| T11 | Debug mode left enabled on a production-facing service (stack traces, admin endpoints) | B | `CDS-SEC-072` | Covered by rule; verify default profile values follow it. |

### 5.3 Container and host runtime

| # | Attack path | Boundary | Current control | Residual risk / owning issue |
| --- | --- | --- | --- | --- |
| T12 | Container runs as root, privilege escalation to host | A | `CDS-SEC-060` (root), `CDS-SEC-061` (privileged/capabilities), automated hardening tests (PR #199) | Covered for CDS-built images; third-party images (e.g. current Postgres/KeyDB upstream, and any future module's upstream image) may still default to root. |
| T13 | Writable root filesystem allows tampering with running container | A | `CDS-SEC-062` | Covered by rule + hardening tests. |
| T14 | Sensitive host path mounted into a container (e.g. `/`, `/etc`, Docker socket) | A | `CDS-SEC-063` | Covered by rule for known patterns. |
| T15 | Missing seccomp/AppArmor confinement, container escapes via kernel syscall abuse | A | None enforced today | **Medium-High** — no declarative confinement profile support. **Owning issue: #207.** |
| T16 | Non-rootless Docker daemon socket exposed, any container-breakout yields host root | A | Documented nowhere yet | **High** for shared/multi-tenant hosts. **Owning issue: #211.** |
| T17 | Host itself unpatched, exposed daemon API, weak file permissions on `.env`/config | A | `CDS-SEC-032` (secret file permissions) is the only host-adjacent rule | **Medium** — no host hardening baseline published. **Owning issue: #211.** |
| T18 | Development web server (e.g. Flask dev server, Django `runserver`) used as the production HTTP entrypoint — no worker model, verbose debug pages, poor concurrency/DoS resistance | A/B | None enforced | **Medium-High.** **Owning issue: #216.** |

### 5.4 Supply chain

| # | Attack path | Boundary | Current control | Residual risk / owning issue |
| --- | --- | --- | --- | --- |
| T19 | Image pulled by `:latest` or unpinned tag, silently changes underneath a deployment | C | `CDS-SEC-050`, `CDS-SEC-053`, `CDS-SEC-054` | Covered by rule. |
| T20 | Image pulled without a digest pin, tag can be repointed at the registry | C | `CDS-SEC-051` | Covered by rule. |
| T21 | Image pulled from an untrusted/typo-squatted registry | C | `CDS-SEC-052`, case-insensitive allowlist (PR #337) | Covered. |
| T22 | Image signature/provenance not verified — a tampered image is deployed with a valid-looking tag/digest | C | `cli/image_verification.py` (CDS-VER-001/002), cosign + fixture fallback (PR #330) | Covered for `full` mode; **gap:** default policy mode may be `off`/`policy` outside production — verify defaults match #206/#216 auth requirements. |
| T23 | New CVE disclosed in an already-deployed base image; no rescanning after initial CI scan | C | One-time scan in CI (`image-security-scan.yml`, PR from #71); no scheduled rescanning | **High** — no continuous rescanning. **Owning issue: #209.** |
| T24 | Compromised CI job forges a signature using the repo's OIDC identity | C | Keyless OIDC cert-identity regexp scoped to `publish-images.yml@refs/heads/main` (fixed in review of #330) | Covered, assuming branch protection prevents arbitrary workflow edits on `main`. |
| T25 | SBOM/vulnerability report tampered with or not retained, hiding a known-bad image after the fact | C | SBOM generation exists (#71); retention/immutability not modeled here | Medium — out of explicit scope for this document, flag for #209. |

### 5.5 Backup / recovery / observability

| # | Attack path | Boundary | Current control | Residual risk / owning issue |
| --- | --- | --- | --- | --- |
| T26 | Ransomware/data-destruction event with no tested restore path | B | None | **High** — no backup contracts exist today. **Owning issue: #210.** |
| T27 | Backup artifact itself contains unencrypted secrets/PII and is exfiltrated | D | None | **High**, same owning issue: #210. |
| T28 | Security-relevant events (auth failures, privilege escalation attempts) not retained long enough to investigate an incident | A/B | Docker's default logging only; no structured retention policy | Medium — tracked separately under #174 (long-term observability), not blocking the Docker Compose production baseline per #64's delivery order. |

## 6. Risk prioritization summary

Ranked by (likelihood × impact), highest first, mapped to the owning
follow-up issue:

1. **T5 / T9 / T10 / T16 / T26 / T27** — no authentication enforcement, no
   network segmentation, no TLS, non-rootless daemon exposure, and no
   backup/restore path are the highest-impact gaps because each allows a
   single compromised or misconfigured service to fully compromise
   confidentiality or availability of the whole stack. Owners: #206, #070,
   #205, #211, #210.
2. **T3 / T4** — dead/buggy security rules (`CDS-SEC-070` never fires,
   `CDS-SEC-010`'s first branch is dead code) give false confidence that
   credential-leak and default-credential classes are caught when they are
   not. Owner: #206 (plus the standalone bug issues #297, #292).
3. **T15 / T18** — missing runtime confinement and production process
   managers increase the blast radius of a successful application-layer
   compromise but require an existing compromise to matter. Owners: #207,
   #216.
4. **T23** — no continuous rescanning means a newly disclosed CVE in an
   already-running image goes undetected indefinitely. Owner: #209.
5. **T17** — host hardening baseline is undocumented; mitigated somewhat by
   T16's rootless requirement once implemented. Owner: #211.

## 7. Penetration-test plan

### 7.1 Scope and rules of engagement

- **Target:** a rendered instance of the production-hardened reference
  profile once #70 lands (or, until then, the closest available
  production-classified profile), deployed to an isolated test host that is
  not shared with any other workload.
- **Out of bounds:** the CDS CLI's own host machine/CI infrastructure,
  third-party SaaS registries (ghcr.io/Docker Hub) beyond read-only
  reconnaissance, and any production data — testing uses synthetic
  data only.
- **Duration/cadence:** run once ahead of the first tagged production
  release, then on every subsequent minor release that touches a control in
  §5, and at minimum annually thereafter.
- **Authorization:** requires written sign-off from the repository owner
  before any active (non-read-only) testing begins, since some tests
  (credential brute-force, DoS) can degrade service availability.

### 7.2 Test phases

1. **Reconnaissance (passive).** Enumerate exposed ports/services from
   outside Boundary A and Boundary B; diff against the profile's declared
   `ports`/`expose` entries to catch any undeclared exposure (validates T8,
   T9).
2. **Secrets and configuration review.** Inspect `docker inspect`, container
   environment, and process list for resolved secrets in command args or
   env vars (validates T2, T3); attempt to read `.env`/config file
   permissions from a non-privileged account on the host (validates T17).
3. **Authentication and authorization testing.** Attempt unauthenticated
   connections to the warehouse, cache, and BI admin UI; attempt default/weak
   credential logins (validates T4, T5, T8).
4. **Network segmentation testing.** From within a low-privilege service
   container (e.g. cache), attempt to reach the warehouse and orchestration
   services directly, bypassing declared contracts (validates T9).
5. **Transport security testing.** Capture traffic on Boundary B (e.g. via a
   test-only network tap) to confirm credentials/tokens are not sent in
   plaintext where TLS is expected (validates T10).
6. **Container escape and privilege testing.** From inside each container,
   attempt privilege escalation, capability abuse, and host filesystem/socket
   access consistent with its declared confinement profile (validates T12,
   T13, T14, T15, T16).
7. **Supply-chain verification testing.** Attempt to deploy a tampered image
   with a forged tag/digest and confirm `cds security --verify-images`
   rejects it; confirm a `latest`/unpinned tag is flagged before reaching
   this phase (validates T19–T22, T24).
8. **Backup/restore drill.** Trigger a full data-loss simulation (destroy the
   warehouse volume) and time the restore against the RPO/RTO targets defined
   once #210 lands (validates T26, T27).
9. **Reporting.** Every finding is recorded with severity (aligned to the
   `high`/`medium`/`low` vocabulary already used by `rule-set.json`), owner,
   remediation deadline, and a retest date; findings are filed as GitHub
   issues linked back to this document and to the owning production-readiness
   issue from §5.

### 7.3 Tooling boundaries

- Network/service testing: standard open-source tools (e.g. `nmap`, `curl`,
  a TLS inspection tool) run from a test-only network segment.
- Container/runtime testing: `docker inspect`, `docker exec`, and
  container-escape test suites run only inside the isolated test host — never
  against a shared or production host.
- Supply-chain testing: reuses CDS's own `cds security --verify-images` and
  `cosign` tooling as the primary detection surface, plus manual crafting of
  a deliberately non-compliant image/fixture to confirm fail-closed behavior
  (mirrors the existing regression-test pattern in
  `tests/test_image_verification.py`).
- No fuzzing or DoS testing against any shared CI or registry infrastructure.

## 8. Findings tracking

Findings from any penetration test or from this document's own review must
be filed as GitHub issues using this template and linked from this section
once opened:

| Field | Description |
| --- | --- |
| Severity | `high` / `medium` / `low`, matching the vocabulary in `cli/resources/rule-set.json` |
| Owner | GitHub handle responsible for remediation |
| Remediation deadline | Target date, scaled to severity (e.g. high: 30 days, medium: 90 days, low: next minor release) |
| Retest status | `not started` / `scheduled` / `passed` / `failed`, updated after each retest |

No findings have been filed against this version of the document yet; this
section will be populated as the first penetration test (§7) is executed.

## 9. Relationship to other production-readiness issues

This document is the security-program prerequisite referenced by #64's
coordinated delivery order:

1. This document (#212) defines the threat model and prioritized risks.
2. #205 (TLS), #206 (authentication), #207 (runtime confinement), #210
   (backup/restore), #211 (host hardening), and #216 (production process
   managers) implement the individual controls against the risks in §5; they
   may proceed in parallel now that this document exists.
3. #217, #229, #230 (environment classification and promotion) are
   independent of this document and do not block on it.
4. #70 integrates the completed controls into the production reference
   profile.
5. #72 verifies and documents the integrated baseline, and should reference
   the penetration-test plan in §7 as part of its reproducible verification
   procedure.
